# Screenshots

`dashboard-preview.svg` is a lightweight repository preview that matches the visual direction of the app.

For a public portfolio repository, replace or add real browser screenshots after running the project locally. A good set would be:

- landing page
- generated study dashboard
- flashcards
- quiz results
- dark mode
